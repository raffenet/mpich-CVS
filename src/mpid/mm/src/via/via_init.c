#include "viaimpl.h"

/*@
   via_init - initialize via method

   Notes:
@*/
int via_init( void )
{
    return MPI_SUCCESS;
}

/*@
   via_finalize - finalize via method

   Notes:
@*/
int via_finalize( void )
{
    return MPI_SUCCESS;
}
